# apps/web — Donaive Core Frontend

El frontend Core **permanece en la raíz del repositorio** (`/src`) para no romper rutas, build ni despliegue actual.

Este directorio documenta la ubicación futura del frontend cuando se ejecute la migración monorepo completa.

**Consumo de contratos:** `@donaive/domain`, `@donaive/core` vía aliases TypeScript/Vite.
