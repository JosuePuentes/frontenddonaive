# apps/ai — Donaive AI Orchestrator

Orquestador de agentes IA **no implementado**.

Contratos preparados en `@donaive/core` (`AgentContext`, `ProjectAccess`, `AgentPermission`).

Regla: prepare/analyze/suggest → libre con authz; publish/execute → aprobación humana.
